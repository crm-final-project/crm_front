import { HeaderBar } from '../../molecules';
import { RegisterForm } from '../../molecules';

export const UserRegForm = (props) => {
  return (
    <>
      <HeaderBar />
      <RegisterForm />
    </>
  );
};
