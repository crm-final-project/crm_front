import { Button } from '@mui/material';

export const BtnLogin = (props) => {

  return (
    <Button type="submit" fullWidth variant="contained" sx={{ mt: 3, mb: 2, backgroundColor: '#2d8254'}}>
      Login
    </Button>
  );
};
